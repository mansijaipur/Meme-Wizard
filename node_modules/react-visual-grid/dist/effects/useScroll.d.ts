/// <reference types="react" />
import { Region, ScrollOptions, ScrollPositions } from "./models";
export default function useScroll({ ref, imageDimensions, gridLayout, resizeStarted, fullScreen, zoomLevel, }: ScrollOptions): {
    endReached: boolean;
    isScrolled: boolean;
    region: Region;
    scrollPercent: number;
    scrollPositions: ScrollPositions;
    scrollToBottom: () => void;
    scrollToTop: () => void;
    setRegion: import("react").Dispatch<import("react").SetStateAction<Region>>;
    startReached: boolean;
};
