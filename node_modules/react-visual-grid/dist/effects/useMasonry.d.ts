import { RefObject } from "react";
import { MasonryProps } from "../react-visual-grid";
type masonryOptions = Pick<MasonryProps, "fillMode" | "gutter" | "enableAnimation" | "animationDelay"> & {
    target?: RefObject<HTMLElement>;
};
export declare function useMasonry({ target, fillMode, gutter, enableAnimation, animationDelay, }: masonryOptions): void;
export {};
