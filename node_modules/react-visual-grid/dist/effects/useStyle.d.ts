import { CSSProperties } from "react";
import { StyleOptions } from "./models";
export declare function useStyle({ rootDimensions, imageDimensions, gridLayout, region, columns, rows, gap, mode, isFullScreen, isResized, }: StyleOptions): {
    galleryStyle: CSSProperties;
    wrapperStyle: CSSProperties;
};
